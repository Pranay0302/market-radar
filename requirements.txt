# MarketRadar dependencies
#
# CORE (required): the full pipeline, evals and tests run on these alone.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
networkx>=3.0

# UI (optional): only needed to run the Streamlit dashboard.
streamlit>=1.43

# OPEN-SOURCE ML MODELS (optional): auto-detected at runtime. If absent, the
# pipeline transparently falls back to deterministic / sklearn backends and
# still runs end-to-end. Install these to activate the real models:
#   - sentence-transformers -> all-MiniLM-L6-v2 spec/review embeddings
#   - transformers + torch  -> distilbert sentiment + flan-t5-base rationale
#   - chromadb              -> vector store for the RAG aspect classifier
#
# These are LEFT UNINSTALLED for deployment: torch + transformers alone exceed
# the ~1GB memory/disk limit on hosts like Streamlit Community Cloud, and the
# app runs fine on the fallbacks without them. Uncomment for local dev to
# activate the real models (see requirements-ml.txt note below).
# sentence-transformers>=2.2
# transformers>=4.38
# torch>=2.0
# chromadb>=0.4

# Test runner
pytest>=7.0
